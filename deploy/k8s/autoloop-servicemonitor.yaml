apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: autoloop
  labels:
    app: autoloop
spec:
  selector:
    matchLabels:
      app: autoloop
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
