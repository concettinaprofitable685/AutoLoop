// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertLearningSessionRecordArgs {
    pub id: String,
    pub session_id: String,
    pub objective: String,
    pub status: String,
    pub priority: f32,
    pub summary: String,
    pub started_at_ms: u64,
    pub completed_at_ms: Option<u64>,
}

impl From<UpsertLearningSessionRecordArgs> for super::Reducer {
    fn from(args: UpsertLearningSessionRecordArgs) -> Self {
        Self::UpsertLearningSessionRecord {
            id: args.id,
            session_id: args.session_id,
            objective: args.objective,
            status: args.status,
            priority: args.priority,
            summary: args.summary,
            started_at_ms: args.started_at_ms,
            completed_at_ms: args.completed_at_ms,
        }
    }
}

impl __sdk::InModule for UpsertLearningSessionRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_learning_session_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_learning_session_record {
    /// Request that the remote module invoke the reducer `upsert_learning_session_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_learning_session_record:upsert_learning_session_record_then`] to run a callback after the reducer completes.
    fn upsert_learning_session_record(
        &self,
        id: String,
        session_id: String,
        objective: String,
        status: String,
        priority: f32,
        summary: String,
        started_at_ms: u64,
        completed_at_ms: Option<u64>,
    ) -> __sdk::Result<()> {
        self.upsert_learning_session_record_then(
            id,
            session_id,
            objective,
            status,
            priority,
            summary,
            started_at_ms,
            completed_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_learning_session_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_learning_session_record_then(
        &self,
        id: String,
        session_id: String,
        objective: String,
        status: String,
        priority: f32,
        summary: String,
        started_at_ms: u64,
        completed_at_ms: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_learning_session_record for super::RemoteReducers {
    fn upsert_learning_session_record_then(
        &self,
        id: String,
        session_id: String,
        objective: String,
        status: String,
        priority: f32,
        summary: String,
        started_at_ms: u64,
        completed_at_ms: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertLearningSessionRecordArgs {
                id,
                session_id,
                objective,
                status,
                priority,
                summary,
                started_at_ms,
                completed_at_ms,
            },
            callback,
        )
    }
}
