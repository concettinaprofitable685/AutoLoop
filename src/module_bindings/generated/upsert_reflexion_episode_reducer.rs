// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertReflexionEpisodeArgs {
    pub id: String,
    pub session_id: String,
    pub objective: String,
    pub hypothesis: String,
    pub outcome: String,
    pub lesson: String,
    pub status: String,
    pub score: f32,
    pub created_at_ms: u64,
}

impl From<UpsertReflexionEpisodeArgs> for super::Reducer {
    fn from(args: UpsertReflexionEpisodeArgs) -> Self {
        Self::UpsertReflexionEpisode {
            id: args.id,
            session_id: args.session_id,
            objective: args.objective,
            hypothesis: args.hypothesis,
            outcome: args.outcome,
            lesson: args.lesson,
            status: args.status,
            score: args.score,
            created_at_ms: args.created_at_ms,
        }
    }
}

impl __sdk::InModule for UpsertReflexionEpisodeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_reflexion_episode`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_reflexion_episode {
    /// Request that the remote module invoke the reducer `upsert_reflexion_episode` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_reflexion_episode:upsert_reflexion_episode_then`] to run a callback after the reducer completes.
    fn upsert_reflexion_episode(
        &self,
        id: String,
        session_id: String,
        objective: String,
        hypothesis: String,
        outcome: String,
        lesson: String,
        status: String,
        score: f32,
        created_at_ms: u64,
    ) -> __sdk::Result<()> {
        self.upsert_reflexion_episode_then(
            id,
            session_id,
            objective,
            hypothesis,
            outcome,
            lesson,
            status,
            score,
            created_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_reflexion_episode` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_reflexion_episode_then(
        &self,
        id: String,
        session_id: String,
        objective: String,
        hypothesis: String,
        outcome: String,
        lesson: String,
        status: String,
        score: f32,
        created_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_reflexion_episode for super::RemoteReducers {
    fn upsert_reflexion_episode_then(
        &self,
        id: String,
        session_id: String,
        objective: String,
        hypothesis: String,
        outcome: String,
        lesson: String,
        status: String,
        score: f32,
        created_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertReflexionEpisodeArgs {
                id,
                session_id,
                objective,
                hypothesis,
                outcome,
                lesson,
                status,
                score,
                created_at_ms,
            },
            callback,
        )
    }
}
